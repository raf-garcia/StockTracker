import React from 'react'

const HeaderTabs = () => {
    return(
    <div className="header-tabs">
        <a href="#">QUOTES</a>
        <a href="#">MARKETS</a>
        <a href="#">WATCHLISTS</a>
    </div>
    );
}

export default HeaderTabs;